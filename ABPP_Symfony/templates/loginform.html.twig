<div class="flex justify-center items-center h-screen bg-blue-500">
  <div class="bg-white rounded-lg shadow-lg p-8">
    <h1 class="text-2xl font-bold mb-8">Page Login</h1>
    {{ form_start(loginform, {'attr': {'class': 'w-full max-w-md'}}) }}
      {{ form_row(loginform.identifiant, {'attr': {'class': 'form-input rounded-lg mb-4 w-full'}}) }}
      {{ form_row(loginform.mdp, {'attr': {'class': 'form-input rounded-lg mb-4 w-full'}}) }}
      {{ form_row(loginform.connexion, {'attr': {'class': 'bg-blue-500 text-white rounded-lg px-4 py-2 hover:bg-blue-700 w-full'}}) }}
  </div>
</div>